apiVersion: v1
kind: Service
metadata:
  name: {{ template "rucio.fullname" . }}-cache
  labels:
    app: {{ template "rucio.name" . }}-cache
    chart: {{ template "rucio.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP 
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: https
  selector:
    app: {{ template "rucio.name" . }}
    release: {{ .Release.Name }}
