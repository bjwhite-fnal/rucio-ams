# Default values for rucio.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

serviceAccountName: useroot
experiment: dev

cacheServerCount: 1

service:
  port: 11211
  targetPort: 11211

image:
  repository: bjwhitefnal/fnal-rucio-cache
  tag: latest
  pullPolicy: Always
